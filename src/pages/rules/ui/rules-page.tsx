import { Page } from '@/shared/ui';
import { PageWrapper } from '@/shared/ui/page-wrapper';

export const RulesPage = () => {
    return (
        <Page>
            <PageWrapper title="Правила"></PageWrapper>
        </Page>
    );
};
