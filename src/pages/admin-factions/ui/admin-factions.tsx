import { Page } from '@/shared/ui';
import { Title } from '@/shared/ui/title';

export const AdminFactions = () => {
    return (
        <Page>
            <Title tier={1}>Фракции</Title>
        </Page>
    );
};
