import { Page } from '@/shared/ui';
import { PageWrapper } from '@/shared/ui/page-wrapper';

export const CharacterPage = () => {
    return (
        <Page>
            <PageWrapper title="Персонаж"></PageWrapper>
        </Page>
    );
};
