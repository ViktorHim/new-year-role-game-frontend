import { Page } from '@/shared/ui';
import { PageWrapper } from '@/shared/ui/page-wrapper';

export const StoryPage = () => {
    return (
        <Page>
            <PageWrapper title="История">Тут будет история игры...</PageWrapper>
        </Page>
    );
};
