import { Page } from '@/shared/ui';
import { Title } from '@/shared/ui/title';

export const AdminHome = () => {
    return (
        <Page>
            <Title tier={1}>Главная</Title>
        </Page>
    );
};
