import { Page } from '@/shared/ui';
import { PageWrapper } from '@/shared/ui/page-wrapper';

export const FractionPage = () => {
    return (
        <Page>
            <PageWrapper title="Фракция"></PageWrapper>
        </Page>
    );
};
